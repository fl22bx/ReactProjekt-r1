import * as cases from '../../helpers/constants'
/**
 * Action empty flash
 *
 * @export
 * @param {any} msg
 * @param {any} flashType
 */
export function emptyFlash () {
  return {
    type: cases.emty
  }
}
